{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": [
    "SESSION_SECRET_KEY",
    "DATABASE_URL",
    "REDIS_URL",
    "REDIS_QUEUE_URL",
    "REDIS_STREAM_URL",
    "NODE_ENV",
    "LOGIN_MODE",
    "ENV"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["build"],
      "cache": false
    },
    "test:integration": {
      "dependsOn": ["build"],
      "cache": false
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "docs": {
      "dependsOn": ["build"],
      "outputs": ["../../api-reference/**"]
    },
    "clean": {
      "cache": false
    }
  },
  "globalDependencies": [".env"]
}
