diff --git a/dist/y-tiptap.cjs b/dist/y-tiptap.cjs
index 653ffe79d5edb4a040555bb222f472af1e45edc8..acbadde198d1d4715872bfc39d669c23cfdc45a2 100644
--- a/dist/y-tiptap.cjs
+++ b/dist/y-tiptap.cjs
@@ -379,6 +379,35 @@ const restoreRelativeSelection = (tr, relSel, binding, oldDoc) => {
       if (selection !== null) {
         tr.setSelection(selection);
       }
+    } else if (relSel.type === 'body') {
+      // PATCH: a selection type of ours, restored by name.
+      //
+      // Without this branch it falls to the `else` below and is rebuilt as a
+      // `TextSelection.between`, which is exactly what BodySelection exists to
+      // avoid: TextSelection endpoints must sit in inline content, so a body
+      // starting or ending with a divider loses that block from the selection
+      // on every remote edit. y-prosemirror#24 reports the same thing for
+      // prosemirror-tables' CellSelection and was closed NOT_PLANNED;
+      // y-prosemirror#266 asks upstream for a general mechanism and is open.
+      // Tiptap's own answer is the `nodeRange` branch just above, added in
+      // ueberdosis/y-tiptap#35. This is the same move for `body`.
+      //
+      // No positions are carried across: BodySelection derives its range from
+      // the document it is given (as AllSelection does), so `fromJSON` needs
+      // the doc and nothing else. That also means there is nothing to clamp.
+      //
+      // The catch is a last resort nothing currently reaches: fromJSON derives
+      // the range and returns a cursor when the body is empty, so it does not
+      // throw. Should it ever throw, the fallback puts a cursor at the start of
+      // the document, which lands INSIDE THE TITLE — measured,
+      // TextSelection.near(resolve(0)) gives 1..1 with the title as its parent.
+      // That is the trade being made: some valid selection beats a broken one,
+      // and leaving the selection unset maps it to the document start anyway.
+      try {
+        tr.setSelection(prosemirrorState.Selection.fromJSON(tr.doc, { type: 'body' }));
+      } catch (e) {
+        tr.setSelection(prosemirrorState.TextSelection.near(tr.doc.resolve(0)));
+      }
     } else {
       let anchor = relativePositionToAbsolutePosition(
         binding.doc,
@@ -927,6 +956,99 @@ const createNodeFromYElement = (
   prevSnapshot,
   computeYChange
 ) => {
+  // PATCH: an element whose NAME this build does not know goes into a fallback
+  // node, instead of being deleted from the shared document.
+  //
+  // Decided here, before the try, rather than inside the catch below. The catch
+  // fires for every construction failure, and most of them have nothing to do
+  // with an unknown vocabulary: two clients on the SAME build, each deleting a
+  // different child of one blockquote, merge into an empty blockquote that
+  // `block+` rejects. Deleting that is y-tiptap healing itself, and answering
+  // it with a fallback would show two fully up-to-date people "unsupported
+  // content" and stop them editing. Asking only "is this name unknown" leaves
+  // every other failure to the repair below, untouched.
+  //
+  // Block or inline is decided by what the PARENT accepts, because the two
+  // positions fail differently: a block-level stand-in inside a paragraph is
+  // not valid content, so the paragraph itself would fail to build and the
+  // whole sentence would go.
+  //
+  // There is a THIRD answer, and leaving it out is what made this destructive:
+  // a parent can accept neither. `title` holds `text*`: text and no node of
+  // any kind, so forcing a stand-in into it
+  // makes the PARENT fail to build, and the parent's failure lands in the
+  // untouched catch below, which deletes the parent — the title and every
+  // character in it, gone from the shared document. Answering "no stand-in
+  // here" instead leaves this one element to that same catch: it alone is
+  // removed — one element lost instead of the parent and every character in
+  // it. Nobody is told: Yjs runs type observers before it emits `update`, so
+  // the intercept reads a document the name has already been removed from.
+  if (schema.nodes[el.nodeName] === undefined) {
+    const parent = el.parent;
+    const parentType =
+      parent instanceof Y.XmlElement ? schema.nodes[parent.nodeName] : undefined;
+    const inlineFallback = schema.nodes.unsupportedInline;
+    const blockFallback = schema.nodes.unsupportedBlock;
+    /**
+     * Whether the parent's content expression has ANY position that takes
+     * this type — not whether its FIRST position does.
+     *
+     * `NodeType.contentMatch` is the match at the START of the expression.
+     * Asking it directly answers "can this be the parent's first child",
+     * which is a different question and answers no far too often: a list
+     * item holds `paragraph block*`, so an unknown block sitting after that
+     * paragraph — where every new block type in a list ends up — got told
+     * "nothing fits here" and was deleted, while the block stand-in is
+     * perfectly legal in the position it actually occupies.
+     *
+     * The match is a state machine, so walk it and ask whether the type is
+     * reachable at all.
+     *
+     * KNOWN IMPRECISION, and the reason it is left here: reachable-anywhere
+     * is not the same as legal-at-THIS-position. A parent with a mandatory
+     * prefix — `listItem` is the only one in this schema — answers yes for a
+     * block, so an unknown element sitting in that first slot would get a
+     * stand-in the parent cannot hold, the parent would fail to build, and
+     * the untouched catch below would delete the parent rather than the one
+     * element. Producing that input needs a build where an unknown type can
+     * be a list item's first child, i.e. a changed `listItem` content rule,
+     * which no version does. The exact question — walk the preceding
+     * siblings, then ask — is tracked rather than written here, because it
+     * has to reason about siblings this build may not recognise either.
+     * @param {any} type
+     * @returns {boolean}
+     */
+    const parentAccepts = (type) => {
+      if (type === undefined || parentType === undefined) return false;
+      const seen = new Set();
+      const queue = [parentType.contentMatch];
+      while (queue.length > 0) {
+        const match = queue.shift();
+        if (match === undefined || match === null || seen.has(match)) continue;
+        seen.add(match);
+        for (let i = 0; i < match.edgeCount; i++) {
+          const edge = match.edge(i);
+          if (edge.type === type) return true;
+          queue.push(edge.next);
+        }
+      }
+      return false
+    };
+    let fallbackType;
+    if (parentType === undefined) {
+      // The parent is the fragment root, which holds blocks.
+      fallbackType = blockFallback;
+    } else if (parentAccepts(inlineFallback)) {
+      fallbackType = inlineFallback;
+    } else if (parentAccepts(blockFallback)) {
+      fallbackType = blockFallback;
+    }
+    if (fallbackType !== undefined) {
+      const fallbackNode = fallbackType.create({ originalName: el.nodeName });
+      meta.mapping.set(el, fallbackNode);
+      return fallbackNode
+    }
+  }
   const children = [];
   /**
    * @param {Y.XmlElement | Y.XmlText} type
@@ -999,6 +1121,9 @@ const createNodeFromYElement = (
     meta.mapping.set(el, node);
     return node
   } catch (e) {
+    // Left exactly as y-tiptap wrote it: a name this build DOES know that still
+    // will not build is a concurrent-edit artefact, and removing it is the
+    // repair. The fallback above never reaches here.
     // an error occured while creating the node. This is probably a result of a concurrent action.
     /** @type {Y.Doc} */ (el.doc).transact((transaction) => {
       /** @type {Y.Item} */ (el._item).delete(transaction);
@@ -1164,13 +1289,23 @@ const equalYTextPText = (ytext, ptexts) => {
 
         const pmark = pmarks.find(/** @param {any} mark */ mark => mark.type.name === markname);
 
+        // PATCHED PROBE: a mark this client does not know is carried by the
+        // fallback mark; it holds the original value, so it counts as equal.
+        if (!pmark) {
+          const carried = pmarks.find(/** @param {any} mark */ mark =>
+            mark.type.name === 'unsupportedMark' && mark.attrs.originalName === yattrname);
+          if (carried) {
+            return true
+          }
+        }
+
         // Ensure the pmark is present in the ptexts before checking equality. When replacing a mark with another mark
         // the pmark will be missing from the ptexts and the equalAttrs will throw an error.
         if (!pmark) {
           return false
         }
 
-        return equalAttrs(attr, pmarks.find(/** @param {any} mark */ mark => mark.type.name === markname)?.attrs)
+        return equalAttrs(attr, pmark.attrs)
       })
     )
 };
@@ -1538,7 +1673,17 @@ const attributesToMarks = (attrs, schema) => {
   const marks = [];
   for (const markName in attrs) {
     // remove hashes if necessary
-    marks.push(schema.mark(yattr2markname(markName), attrs[markName]));
+    const resolvedName = yattr2markname(markName);
+    // PATCHED PROBE: an unknown mark is carried by a fallback mark that keeps
+    // the original key and value, so a round trip through this client is lossless.
+    if (schema.marks[resolvedName] === undefined) {
+      const fallbackMarkType = schema.marks.unsupportedMark;
+      if (fallbackMarkType !== undefined) {
+        marks.push(fallbackMarkType.create({ originalName: markName, originalValue: attrs[markName] }));
+      }
+      continue
+    }
+    marks.push(schema.mark(resolvedName, attrs[markName]));
   }
   return marks
 };
@@ -1550,6 +1695,11 @@ const attributesToMarks = (attrs, schema) => {
 const marksToAttributes = (marks, meta) => {
   const pattrs = {};
   marks.forEach((mark) => {
+    if (mark.type.name === 'unsupportedMark') {
+      // PATCHED PROBE: put the original key and value back untouched.
+      pattrs[mark.attrs.originalName] = mark.attrs.originalValue;
+      return
+    }
     if (mark.type.name !== 'ychange') {
       const isOverlapping = map__namespace.setIfUndefined(meta.isOMark, mark.type, () => !mark.type.excludes(mark.type));
       pattrs[isOverlapping ? `${mark.type.name}--${hashOfJSON(mark.toJSON())}` : mark.type.name] = mark.attrs;
diff --git a/dist/y-tiptap.js b/dist/y-tiptap.js
index 28c55820dff13cf3d5c303970f530c17eb38f757..a3c6fe3555dfd2d39d75bc049cdab1d6de195433 100644
--- a/dist/y-tiptap.js
+++ b/dist/y-tiptap.js
@@ -345,6 +345,35 @@ const restoreRelativeSelection = (tr, relSel, binding, oldDoc) => {
       if (selection !== null) {
         tr.setSelection(selection);
       }
+    } else if (relSel.type === 'body') {
+      // PATCH: a selection type of ours, restored by name.
+      //
+      // Without this branch it falls to the `else` below and is rebuilt as a
+      // `TextSelection.between`, which is exactly what BodySelection exists to
+      // avoid: TextSelection endpoints must sit in inline content, so a body
+      // starting or ending with a divider loses that block from the selection
+      // on every remote edit. y-prosemirror#24 reports the same thing for
+      // prosemirror-tables' CellSelection and was closed NOT_PLANNED;
+      // y-prosemirror#266 asks upstream for a general mechanism and is open.
+      // Tiptap's own answer is the `nodeRange` branch just above, added in
+      // ueberdosis/y-tiptap#35. This is the same move for `body`.
+      //
+      // No positions are carried across: BodySelection derives its range from
+      // the document it is given (as AllSelection does), so `fromJSON` needs
+      // the doc and nothing else. That also means there is nothing to clamp.
+      //
+      // The catch is a last resort nothing currently reaches: fromJSON derives
+      // the range and returns a cursor when the body is empty, so it does not
+      // throw. Should it ever throw, the fallback puts a cursor at the start of
+      // the document, which lands INSIDE THE TITLE — measured,
+      // TextSelection.near(resolve(0)) gives 1..1 with the title as its parent.
+      // That is the trade being made: some valid selection beats a broken one,
+      // and leaving the selection unset maps it to the document start anyway.
+      try {
+        tr.setSelection(Selection.fromJSON(tr.doc, { type: 'body' }));
+      } catch (e) {
+        tr.setSelection(TextSelection.near(tr.doc.resolve(0)));
+      }
     } else {
       let anchor = relativePositionToAbsolutePosition(
         binding.doc,
@@ -893,6 +922,99 @@ const createNodeFromYElement = (
   prevSnapshot,
   computeYChange
 ) => {
+  // PATCH: an element whose NAME this build does not know goes into a fallback
+  // node, instead of being deleted from the shared document.
+  //
+  // Decided here, before the try, rather than inside the catch below. The catch
+  // fires for every construction failure, and most of them have nothing to do
+  // with an unknown vocabulary: two clients on the SAME build, each deleting a
+  // different child of one blockquote, merge into an empty blockquote that
+  // `block+` rejects. Deleting that is y-tiptap healing itself, and answering
+  // it with a fallback would show two fully up-to-date people "unsupported
+  // content" and stop them editing. Asking only "is this name unknown" leaves
+  // every other failure to the repair below, untouched.
+  //
+  // Block or inline is decided by what the PARENT accepts, because the two
+  // positions fail differently: a block-level stand-in inside a paragraph is
+  // not valid content, so the paragraph itself would fail to build and the
+  // whole sentence would go.
+  //
+  // There is a THIRD answer, and leaving it out is what made this destructive:
+  // a parent can accept neither. `title` holds `text*`: text and no node of
+  // any kind, so forcing a stand-in into it
+  // makes the PARENT fail to build, and the parent's failure lands in the
+  // untouched catch below, which deletes the parent — the title and every
+  // character in it, gone from the shared document. Answering "no stand-in
+  // here" instead leaves this one element to that same catch: it alone is
+  // removed — one element lost instead of the parent and every character in
+  // it. Nobody is told: Yjs runs type observers before it emits `update`, so
+  // the intercept reads a document the name has already been removed from.
+  if (schema.nodes[el.nodeName] === undefined) {
+    const parent = el.parent;
+    const parentType =
+      parent instanceof Y.XmlElement ? schema.nodes[parent.nodeName] : undefined;
+    const inlineFallback = schema.nodes.unsupportedInline;
+    const blockFallback = schema.nodes.unsupportedBlock;
+    /**
+     * Whether the parent's content expression has ANY position that takes
+     * this type — not whether its FIRST position does.
+     *
+     * `NodeType.contentMatch` is the match at the START of the expression.
+     * Asking it directly answers "can this be the parent's first child",
+     * which is a different question and answers no far too often: a list
+     * item holds `paragraph block*`, so an unknown block sitting after that
+     * paragraph — where every new block type in a list ends up — got told
+     * "nothing fits here" and was deleted, while the block stand-in is
+     * perfectly legal in the position it actually occupies.
+     *
+     * The match is a state machine, so walk it and ask whether the type is
+     * reachable at all.
+     *
+     * KNOWN IMPRECISION, and the reason it is left here: reachable-anywhere
+     * is not the same as legal-at-THIS-position. A parent with a mandatory
+     * prefix — `listItem` is the only one in this schema — answers yes for a
+     * block, so an unknown element sitting in that first slot would get a
+     * stand-in the parent cannot hold, the parent would fail to build, and
+     * the untouched catch below would delete the parent rather than the one
+     * element. Producing that input needs a build where an unknown type can
+     * be a list item's first child, i.e. a changed `listItem` content rule,
+     * which no version does. The exact question — walk the preceding
+     * siblings, then ask — is tracked rather than written here, because it
+     * has to reason about siblings this build may not recognise either.
+     * @param {any} type
+     * @returns {boolean}
+     */
+    const parentAccepts = (type) => {
+      if (type === undefined || parentType === undefined) return false;
+      const seen = new Set();
+      const queue = [parentType.contentMatch];
+      while (queue.length > 0) {
+        const match = queue.shift();
+        if (match === undefined || match === null || seen.has(match)) continue;
+        seen.add(match);
+        for (let i = 0; i < match.edgeCount; i++) {
+          const edge = match.edge(i);
+          if (edge.type === type) return true;
+          queue.push(edge.next);
+        }
+      }
+      return false
+    };
+    let fallbackType;
+    if (parentType === undefined) {
+      // The parent is the fragment root, which holds blocks.
+      fallbackType = blockFallback;
+    } else if (parentAccepts(inlineFallback)) {
+      fallbackType = inlineFallback;
+    } else if (parentAccepts(blockFallback)) {
+      fallbackType = blockFallback;
+    }
+    if (fallbackType !== undefined) {
+      const fallbackNode = fallbackType.create({ originalName: el.nodeName });
+      meta.mapping.set(el, fallbackNode);
+      return fallbackNode
+    }
+  }
   const children = [];
   /**
    * @param {Y.XmlElement | Y.XmlText} type
@@ -965,6 +1087,9 @@ const createNodeFromYElement = (
     meta.mapping.set(el, node);
     return node
   } catch (e) {
+    // Left exactly as y-tiptap wrote it: a name this build DOES know that still
+    // will not build is a concurrent-edit artefact, and removing it is the
+    // repair. The fallback above never reaches here.
     // an error occured while creating the node. This is probably a result of a concurrent action.
     /** @type {Y.Doc} */ (el.doc).transact((transaction) => {
       /** @type {Y.Item} */ (el._item).delete(transaction);
@@ -1130,13 +1255,23 @@ const equalYTextPText = (ytext, ptexts) => {
 
         const pmark = pmarks.find(/** @param {any} mark */ mark => mark.type.name === markname);
 
+        // PATCHED PROBE: a mark this client does not know is carried by the
+        // fallback mark; it holds the original value, so it counts as equal.
+        if (!pmark) {
+          const carried = pmarks.find(/** @param {any} mark */ mark =>
+            mark.type.name === 'unsupportedMark' && mark.attrs.originalName === yattrname);
+          if (carried) {
+            return true
+          }
+        }
+
         // Ensure the pmark is present in the ptexts before checking equality. When replacing a mark with another mark
         // the pmark will be missing from the ptexts and the equalAttrs will throw an error.
         if (!pmark) {
           return false
         }
 
-        return equalAttrs(attr, pmarks.find(/** @param {any} mark */ mark => mark.type.name === markname)?.attrs)
+        return equalAttrs(attr, pmark.attrs)
       })
     )
 };
@@ -1504,7 +1639,17 @@ const attributesToMarks = (attrs, schema) => {
   const marks = [];
   for (const markName in attrs) {
     // remove hashes if necessary
-    marks.push(schema.mark(yattr2markname(markName), attrs[markName]));
+    const resolvedName = yattr2markname(markName);
+    // PATCHED PROBE: an unknown mark is carried by a fallback mark that keeps
+    // the original key and value, so a round trip through this client is lossless.
+    if (schema.marks[resolvedName] === undefined) {
+      const fallbackMarkType = schema.marks.unsupportedMark;
+      if (fallbackMarkType !== undefined) {
+        marks.push(fallbackMarkType.create({ originalName: markName, originalValue: attrs[markName] }));
+      }
+      continue
+    }
+    marks.push(schema.mark(resolvedName, attrs[markName]));
   }
   return marks
 };
@@ -1516,6 +1661,11 @@ const attributesToMarks = (attrs, schema) => {
 const marksToAttributes = (marks, meta) => {
   const pattrs = {};
   marks.forEach((mark) => {
+    if (mark.type.name === 'unsupportedMark') {
+      // PATCHED PROBE: put the original key and value back untouched.
+      pattrs[mark.attrs.originalName] = mark.attrs.originalValue;
+      return
+    }
     if (mark.type.name !== 'ychange') {
       const isOverlapping = map.setIfUndefined(meta.isOMark, mark.type, () => !mark.type.excludes(mark.type));
       pattrs[isOverlapping ? `${mark.type.name}--${hashOfJSON(mark.toJSON())}` : mark.type.name] = mark.attrs;
