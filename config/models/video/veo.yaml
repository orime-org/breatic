# VEO Video Models (Google) — 3.1, 3.1 Lite
# Guidelines:
#   model.description    — max 40 chars. No panel shows it: the picker draws
#                          display_name. Its one reader is the canvas capability
#                          answer, which takes it when guide is empty.
#   model.guide          — what this model is for, in prose. The Skill prompt
#                          quotes it whole, to help the model choose; so does
#                          the canvas capability answer, for the models a canvas
#                          generation mode can reach, beside a parameter list
#                          built from the declarations. So a sentence
#                          here the catalog or the panel can prove wrong is a
#                          promise nothing keeps: a slot this entry does not
#                          declare, a capability no control on this node
#                          reaches, a span of seconds its own duration denies,
#                          a "cheapest" or "slowest" its credits and
#                          generation_time deny. Judgement calls ("best for
#                          portraits") are the point of the field and stay.
#                          Guarded by domain's guides-name-what-the-model-takes
#                          test, over the models a canvas generation mode can
#                          reach.
#   model.mode           — t2v, i2v, extend
#   param.description    — max 50 chars. No panel shows it either: the controls
#                          take their labels from the locales. It is what the
#                          canvas capability answer says this param is for.
#   param.default        — every param MUST have a default (used as fallback for invalid values)
#   param names          — user-facing; model family code converts to API format
#   provider.priority    — lower number = tried first
#   cost_per_call        — estimated credits per call (1 credit = 1 US cent). Not
#                          display only: the pre-enqueue balance gate reads it and
#                          refuses with a 402 when the studio holds less. What is
#                          charged is taken afterwards, on actual usage.
#   generation_time      — max generation time in seconds (worst case)


models:
  # -- VEO 3.1 -----------------------------------------------------------------

  - name: "veo-3.1"
    display_name: "VEO 3.1"
    mode: "t2v"
    takes_prompt: true
    tier: recommended
    description: "Google's latest, built-in audio"
    guide: "Google's latest text-to-video with built-in audio generation. Duration 4-8s, up to 1080p. Excellent for realistic scenes with synchronized sound."
    cost_per_call: 100
    generation_time: 180
    params:
      aspect_ratio:
        description: "Output aspect ratio"
        values: ["16:9", "9:16"]
        default: "16:9"
        fill: panel
      resolution:
        description: "Output resolution"
        values: ["720p", "1080p"]
        default: "720p"
        fill: panel
      duration:
        description: "Video length in seconds"
        values: [4, 6, 8]
        default: 8
        fill: panel
      generate_audio:
        description: "Generate audio track"
        values: [true, false]
        default: true
        fill: panel
      negative_prompt:
        description: "Elements to avoid"
        default: null
        fill: none
        note: "the prompt editor is the one place a reader writes words"
      seed:
        description: "Random seed for reproducibility"
        default: -1
        fill: none
        note: "reproducibility plumbing; the product does not expose it"
    providers:
      - name: google
        model_id: "veo-3.1"
        priority: 1
      - name: wavespeed
        model_id: "google/veo3.1/text-to-video"
        priority: 2

  - name: "veo-3.1-i2v"
    display_name: "VEO 3.1 I2V"
    mode: "i2v"
    takes_prompt: true
    tier: recommended
    description: "VEO 3.1 image-to-video"
    guide: "Animate an image into a video using Google VEO 3.1. Supports audio generation. Duration 4-8s, up to 1080p."
    cost_per_call: 100
    generation_time: 180
    params:
      image:
        description: "First frame image URL"
        default: null
        fill: canvas
        accepts: image
      aspect_ratio:
        description: "Output aspect ratio"
        values: ["16:9", "9:16"]
        default: "16:9"
        fill: panel
      resolution:
        description: "Output resolution"
        values: ["720p", "1080p"]
        default: "720p"
        fill: panel
      duration:
        description: "Video length in seconds"
        values: [4, 6, 8]
        default: 8
        fill: panel
      generate_audio:
        description: "Generate audio track"
        values: [true, false]
        default: true
        fill: panel
      negative_prompt:
        description: "Elements to avoid"
        default: null
        fill: none
        note: "the prompt editor is the one place a reader writes words"
      seed:
        description: "Random seed for reproducibility"
        default: -1
        fill: none
        note: "reproducibility plumbing; the product does not expose it"
    providers:
      - name: google
        model_id: "veo-3.1"
        priority: 1
      - name: wavespeed
        model_id: "google/veo3.1/image-to-video"
        priority: 2

  # -- VEO 3.1 Extend ----------------------------------------------------------

  - name: "veo-3.1-extend"
    display_name: "VEO 3.1 Extend"
    mode: "extend"
    takes_prompt: true
    tier: recommended
    description: "VEO 3.1 video extension"
    guide: "Extend an existing video using Google VEO 3.1. Continue the scene with consistent style. Up to 1080p."
    cost_per_call: 100
    generation_time: 180
    params:
      video:
        description: "Source video URL (required)"
        default: null
        fill: none
        accepts: video
        note: "no generation panel reaches this model"
      resolution:
        description: "Output resolution"
        values: ["720p", "1080p"]
        default: "720p"
        fill: none
        note: "no generation panel reaches this model"
      negative_prompt:
        description: "Elements to avoid"
        default: null
        fill: none
        note: "no generation panel reaches this model"
      seed:
        description: "Random seed for reproducibility"
        default: -1
        fill: none
        note: "no generation panel reaches this model"
    providers:
      - name: google
        model_id: "veo-3.1"
        priority: 1
      - name: wavespeed
        model_id: "google/veo3.1-fast/video-extend"
        priority: 2

  # -- VEO 3.1 Fast ------------------------------------------------------------

  - name: "veo-3.1-fast"
    display_name: "VEO 3.1 Fast"
    mode: "t2v"
    takes_prompt: true
    tier: recommended
    description: "VEO 3.1 fast variant"
    guide: "Google VEO 3.1 Fast text-to-video. Same quality at faster generation. Duration 4-8s, up to 1080p."
    cost_per_call: 80
    generation_time: 120
    params:
      aspect_ratio:
        description: "Output aspect ratio"
        values: ["16:9", "9:16"]
        default: "16:9"
        fill: panel
      resolution:
        description: "Output resolution"
        values: ["720p", "1080p"]
        default: "720p"
        fill: panel
      duration:
        description: "Video length in seconds"
        values: [4, 6, 8]
        default: 8
        fill: panel
      generate_audio:
        description: "Generate audio track"
        values: [true, false]
        default: true
        fill: panel
      negative_prompt:
        description: "Elements to avoid"
        default: null
        fill: none
        note: "the prompt editor is the one place a reader writes words"
      seed:
        description: "Random seed for reproducibility"
        default: -1
        fill: none
        note: "reproducibility plumbing; the product does not expose it"
    providers:
      - name: wavespeed
        model_id: "google/veo3.1-fast/text-to-video"
        priority: 1

  # -- VEO 3.1 Lite (NEW — cheapest Google video) ------------------------------

  - name: "veo-3.1-lite"
    display_name: "VEO 3.1 Lite"
    mode: "t2v"
    takes_prompt: true
    tier: optional
    description: "Cheapest Google video, budget tier"
    guide: "Google's most affordable video model. Lower quality than VEO 3.1 but significantly cheaper. Good for drafts, iterations, and cost-sensitive projects. Duration 4-8s."
    cost_per_call: 30
    generation_time: 90
    params:
      aspect_ratio:
        description: "Output aspect ratio"
        values: ["16:9", "9:16"]
        default: "16:9"
        fill: panel
      resolution:
        description: "Output resolution"
        values: ["720p"]
        default: "720p"
        fill: panel
      duration:
        description: "Video length in seconds"
        values: [4, 6, 8]
        default: 8
        fill: panel
      generate_audio:
        description: "Generate audio track"
        values: [true, false]
        default: true
        fill: panel
      negative_prompt:
        description: "Elements to avoid"
        default: null
        fill: none
        note: "the prompt editor is the one place a reader writes words"
      seed:
        description: "Random seed for reproducibility"
        default: -1
        fill: none
        note: "reproducibility plumbing; the product does not expose it"
    providers:
      - name: wavespeed
        model_id: "google/veo3.1-lite/text-to-video"
        priority: 1
