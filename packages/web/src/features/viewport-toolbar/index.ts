/**
 * `features/viewport-toolbar` — bottom-right canvas zoom / fit /
 * minimap / snap controls (spec/02 §4.6 v13). Replaces the v12-era
 * `spaces/canvas/common/UndoRedoToolbar` which was deleted in F8.
 */
export { default as ViewportToolbar } from './ViewportToolbar';
