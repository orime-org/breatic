{
  "name": "@breatic/ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "deploy:worker": "pnpm --filter @breatic/shared build && wrangler deploy --env production",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit && tsc --noEmit -p container/tsconfig.json"
  },
  "dependencies": {
    "@breatic/shared": "workspace:*",
    "@cloudflare/containers": "0.3.7"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.12.21",
    "@cloudflare/workers-types": "^5.20260830.1",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.127.1",
    "@types/node": "catalog:"
  }
}
