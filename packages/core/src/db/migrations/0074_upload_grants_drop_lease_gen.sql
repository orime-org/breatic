-- A node carries several tasks at once (#186), so an upload's grant fences
-- nothing: its outcome settles the row the ticket opened and the node's four
-- counts are recomputed from the table.
ALTER TABLE "upload_grants" DROP COLUMN "lease_gen";
