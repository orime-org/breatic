# 2026-04 月度归档 · 已关闭 bug

本月从 Round 2/3/4 backlog 关闭的 bug 记录。格式:`[x] BUG-XXX · 修复 commit · 关闭日期 · 核查说明`。

> **注**:Round 1 的 29 个 bug 另见独立归档 [`2026-04-15-round-1-closed.md`](2026-04-15-round-1-closed.md)。

---

## 2026-04-21 — Round 4 审计核查关闭

- [x] **BUG-046** · WebSocket token 硬编码 `'dev'` · 修复 commit: `3a811dd`(PR #113)
  - **核查方法**:`git show origin/main:packages/web/src/utils/yjsManager.ts` + `git show origin/main:packages/collab/src/auth.ts`
  - **结论**:前端 `yjsManager.ts:43,87` 两处 `token: 'dev'` 替换为显式 `token` 参数;后端 `collab/auth.ts` 新增 authz 校验(`projects.user_id = session.userId` SQL 验证 + docName 格式校验);Web 侧 hardcoded `'dev'` 清零
  - **遗留提醒**:协作 authz 只支持单所有者(`project_collaborators` 表尚未引入)——与 schema 设计一致,非 bug
  - 详见 Round 4 审计 [修复核查表](2026-04-21-round-4-found.md#修复核查表)

- [x] **BUG-047** · `deductOnce` refKey 无校验 · 修复 commit: `8273383`(PR #125)
  - **核查方法**:`git show 8273383 -- packages/core/src/modules/credit.service.ts`
  - **结论(函数层面)**:`REFKEY_PATTERN = /^[A-Za-z0-9_:.-]{1,255}$/` + 入口 `throw new ValidationError` + 15 条测试用例(空/空格/unicode/len256 全拒绝)
  - **⚠️ 关联**:函数硬化 ≠ 漏洞关闭 — `deductOnce` 在生产零调用,真实幂等缺口转为 **BUG-079**(P0,Round 4 发现)

- [x] **BUG-048** · `deductOnce` 不校验 userId · 修复 commit: `8273383`(PR #125)
  - **核查方法**:同 BUG-047
  - **结论(函数层面)**:lockKey 改为 `${env.ENV}:bill:${userId}:${refKey}`,userId 进入作用域,value 改占位 `"1"`
  - **⚠️ 关联**:同 BUG-047,函数硬化有效但实际 deductOnce 无调用(BUG-079)

- [x] **BUG-052** · `nodeHistory.userId` FK 缺 `onDelete` · 修复 commit: `8273383`(PR #125)
  - **核查方法**:`git show 8273383 -- packages/core/src/db/schema.ts packages/core/src/db/migrations/0008_mean_jubilee.sql`
  - **结论(部分修)**:nodeHistory 自身 FK 已加 `{ onDelete: "restrict" }`,migration 0008 DROP + ADD 正确
  - **⚠️ commit message 不准**:自称 "all other FKs declared explicitly" — 核查后发现 `conversation_attachments.user_id` 与 `project_memory_entries.author_id` 两个兄弟 FK 仍 `no action`,独立追踪为 **BUG-080**(P1 MED)

- [x] **BUG-053** · Stripe webhook secret 可为空字符串 → 签名绕过 · 修复 commit: `8273383`(PR #125)
  - **核查方法**:`git show 8273383 -- packages/core/src/config/env.ts packages/core/src/infra/stripe.ts`
  - **结论**:`env.ts:168-186` `PAYMENT_ENABLED=true` 时在启动期校验 `STRIPE_SECRET_KEY.trim()` 和 `STRIPE_WEBHOOK_SECRET.trim()`,空或空白 → FATAL throw 阻止 boot;`stripe.ts:45` `verifyWebhookSignature` 也 `.trim()` 做纵深防御

---

## 2026-04-22 — PR #126 Batch B 核查关闭

- [x] **BUG-031** · `deleteProject` 不级联软删子记录 · 修复 commit: `e282686`(PR #126)
  - **核查方法**:`git show e282686 -- packages/core/src/modules/project.repo.ts packages/core/src/db/schema.ts`
  - **结论**:`deleteProject` 重写为单事务,级联软删 6 张子表(conversations / nodeHistory / tasks / projectMemories / projectMemoryEntries / yjsDocuments)。每个 UPDATE 用 `isNull(deletedAt)` 防覆盖。yjsDocuments 按 `name LIKE 'project-{id}/%'` 匹配(无 FK 但有 name 前缀约定)
  - **顺带修**:`node_history` 和 `yjs_documents` 补齐 `deletedAt` 列(原来只有 `updatedAt`),migration `0009_gray_tempest.sql` 幂等 ADD COLUMN
  - **未附带修**:BUG-073(`creditTransactions` 缺 `deletedAt`)—— 仍活跃,与本次无关
  - **测试**:无新 unit test 直接覆盖 cascade,但 schema 和 repository 层改动靠 typecheck + 现有 list 查询的 `isNull(deletedAt)` 兜底

- [x] **BUG-033** · Canvas task 创建顺序错(孤儿 task)· 修复 commit: `e282686`(PR #126)
  - **核查方法**:`git show e282686 -- packages/server/src/routes/canvas.ts packages/core/src/modules/task.service.ts packages/server/src/__tests__/routes/canvas.rollback.test.ts`
  - **结论**:锁失败路径(`!acquired`)在 `throw ConflictError` 前加 `await taskService.softDelete(task.id);` 回滚刚创建的 task。新增 `taskService.softDelete()` 封装(包装 `taskRepo.softDeleteTask`)。测试 `canvas.rollback.test.ts` 覆盖两条路径:锁失败 → softDelete 被调用 + 409;锁成功 → softDelete 不被调用 + 正常入队
  - **trade-off 注**:选了最小改动路径(先建后锁,失败回滚),PR 注释明确标明更彻底的 "先锁后建" 需要 taskService.create 接受客户端 taskId 的更大重构,作为 follow-up

---

## 2026-04-22 — Round 5 核查关闭

- [x] **BUG-044** · `schema.ts` cascade 注释过时 · 无单独修复 commit(历次 schema 改动时自然同步)
  - **核查方法**:Agent G(Round 5)在 schema 全扫时核对 `packages/core/src/db/schema.ts` 所有 cascade/restrict 注释
  - **结论**:注释已与代码同步,`onDelete: "restrict"` / `"cascade"` / `"set null"` 在对应 FK 上准确反映,无过时描述
  - **关联**:Round 5 发现 FK 策略自身系统性不一致(BUG-144),但这与 BUG-044 "注释过时" 无关 —— BUG-044 关闭,BUG-144 独立追踪

### ⚠️ BUG-031 补丁不完整备注

虽然 BUG-031 已在 2026-04-22 Round 4 核查中关闭(PR #126 将 `deleteProject` 改为事务级联),但 **Round 5 Agent G 核查中发现级联遗漏 5 张子表**:

- `conversation_attachments`
- `conversation_memories`
- `memory_history_entries`
- 2 张以 `source_conversation_id` 关联的表

BUG-031 原描述("完全不级联")已解决,**遗留缺口单独追踪为 BUG-142(P0)**。本条目不 reopen BUG-031,但审计 session 应注意:**关闭一个 bug 时核查要看所有可能受影响的 sibling records**,而非仅 commit 声明的那一组。

---

## 2026-04-22 — PR #128 Batch C 核查关闭

- [x] **BUG-079** · `deductOnce` 在生产零调用点 —— 3 条扣费路径仍非幂等 · 修复 commit: `710c9e8`(PR #128)
  - **核查方法**:`git show 710c9e8 -- packages/core/src/agent/tools/spawn.ts packages/server/src/agent/main-agent.ts packages/core/src/modules/text-tool.service.ts packages/server/src/routes/text-tools.ts packages/core/src/modules/conversation.repo.ts` + `grep -rnE "creditService\.deduct\(" packages/ --include="*.ts"` 最终核查
  - **结论**:3 条非任务路径全部迁移到 `deductOnce`,refKey 方案统一:
    - Agent chat:`turn:${conversationId}:${turnIndex}`
    - SubAgent spawn:`spawn:${conversationId}:${turnIndex}:${spawnIdx}`
    - Text mini-tool:`texttool:${idempotencyKey}`(支持客户端 `Idempotency-Key` header)
  - **管道改动**:`conversation.repo.addMessage` 改为返回 turnIndex(消除调用方重查 `getCurrentTurnIndex` 的并发竞态);`RequestStore` 加 `billing: { turnIndex, spawnCount }` 字段,MainAgent 在 addMessage 后填充
  - **生产剩余 `deduct()` 调用**:仅 `packages/worker/src/handlers.ts:214` 一处 task 路径 —— 靠 `tasks.billedAt` CAS 做持久化幂等,Round 4 audit 明确说这条路 OK,PR #128 commit message 也声明保留
  - **PR 作者自检**:commit message 写了 CLAUDE.md 规则 #5("这个修复解决根本问题,还是压住了症状?")的失败案例自省 —— Batch A 加固 `deductOnce` 时没 grep 调用方,所以给死函数做加固。新习惯:**每次改 helper 必须 grep 调用方**
  - **未顺带修**(预期中,scope 不同):BUG-112(SSE abort)· BUG-050(spawn 深度)· BUG-061/062(addCredits/deductCredits 负数)· BUG-083(referenceId pattern,由于生产调用点收敛,风险面缩小)

---

## 2026-04-23 — Round 6 核查关闭(技术关闭)

- [x] **BUG-093** · imageEditor 把 nodeId 当 workflowId → authz 100% 拒绝 · **技术关闭**(无修复 commit — feat/video-editor 重构删除 imageEditor 目录)
  - **核查方法**:`git ls-tree origin/main -r packages/web/src/apps/project/components/imageEditor/`(空,文件删除)+ `git grep -n useYjsStore origin/main -- 'packages/web/src/**'`
  - **结论**:`imageEditor/index.tsx` 整个目录在 PR #135(`feat/video-editor`)重构中被删除。BUG-093 原描述("imageEditor 把 nodeId 当 workflowId")的具体文件已不存在,**技术关闭**
  - **⚠️ pattern 迁移**:**Round 6 Agent I 核查发现 bug pattern 完整迁移到新的 `mixedEditor/index.tsx:923`** (`useYjsStore({ id: nodeId, ... })`),且因 mixedEditor 现在支持多节点类型(text / audio / video / image),影响面**扩大而非消除**。新开 **BUG-164**(P0)独立追踪
  - **Audit lesson**:仅凭"文件删除"不能真正关闭 bug。关闭时必须 `git grep` 确认 bug pattern 没有被搬到新代码里。这次 audit 如果只靠 file 存在性判断,就会错误地把 BUG-093 算"已修",实际 pattern 更严重
  - **合规性**:按本分支 policy,BUG-093 从 BUGS.md 移除 + 追加本条归档;同时新开 BUG-164 保持追踪连续性
