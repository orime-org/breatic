# 2026-04 月度归档 · 已关闭 bug

本月从 Round 2/3/4 backlog 关闭的 bug 记录。格式:`[x] BUG-XXX · 修复 commit · 关闭日期 · 核查说明`。

> **注**:Round 1 的 29 个 bug 另见独立归档 [`2026-04-15-round-1-closed.md`](2026-04-15-round-1-closed.md)。

---

## 2026-04-21 — Round 4 审计核查关闭

- [x] **BUG-046** · WebSocket token 硬编码 `'dev'` · 修复 commit: `3a811dd`(PR #113)
  - **核查方法**:`git show origin/main:packages/web/src/utils/yjsManager.ts` + `git show origin/main:packages/collab/src/auth.ts`
  - **结论**:前端 `yjsManager.ts:43,87` 两处 `token: 'dev'` 替换为显式 `token` 参数;后端 `collab/auth.ts` 新增 authz 校验(`projects.user_id = session.userId` SQL 验证 + docName 格式校验);Web 侧 hardcoded `'dev'` 清零
  - **遗留提醒**:协作 authz 只支持单所有者(`project_collaborators` 表尚未引入)——与 schema 设计一致,非 bug
  - 详见 Round 4 审计 [修复核查表](2026-04-21-round-4-found.md#修复核查表)

- [x] **BUG-047** · `deductOnce` refKey 无校验 · 修复 commit: `8273383`(PR #125)
  - **核查方法**:`git show 8273383 -- packages/core/src/modules/credit.service.ts`
  - **结论(函数层面)**:`REFKEY_PATTERN = /^[A-Za-z0-9_:.-]{1,255}$/` + 入口 `throw new ValidationError` + 15 条测试用例(空/空格/unicode/len256 全拒绝)
  - **⚠️ 关联**:函数硬化 ≠ 漏洞关闭 — `deductOnce` 在生产零调用,真实幂等缺口转为 **BUG-079**(P0,Round 4 发现)

- [x] **BUG-048** · `deductOnce` 不校验 userId · 修复 commit: `8273383`(PR #125)
  - **核查方法**:同 BUG-047
  - **结论(函数层面)**:lockKey 改为 `${env.ENV}:bill:${userId}:${refKey}`,userId 进入作用域,value 改占位 `"1"`
  - **⚠️ 关联**:同 BUG-047,函数硬化有效但实际 deductOnce 无调用(BUG-079)

- [x] **BUG-052** · `nodeHistory.userId` FK 缺 `onDelete` · 修复 commit: `8273383`(PR #125)
  - **核查方法**:`git show 8273383 -- packages/core/src/db/schema.ts packages/core/src/db/migrations/0008_mean_jubilee.sql`
  - **结论(部分修)**:nodeHistory 自身 FK 已加 `{ onDelete: "restrict" }`,migration 0008 DROP + ADD 正确
  - **⚠️ commit message 不准**:自称 "all other FKs declared explicitly" — 核查后发现 `conversation_attachments.user_id` 与 `project_memory_entries.author_id` 两个兄弟 FK 仍 `no action`,独立追踪为 **BUG-080**(P1 MED)

- [x] **BUG-053** · Stripe webhook secret 可为空字符串 → 签名绕过 · 修复 commit: `8273383`(PR #125)
  - **核查方法**:`git show 8273383 -- packages/core/src/config/env.ts packages/core/src/infra/stripe.ts`
  - **结论**:`env.ts:168-186` `PAYMENT_ENABLED=true` 时在启动期校验 `STRIPE_SECRET_KEY.trim()` 和 `STRIPE_WEBHOOK_SECRET.trim()`,空或空白 → FATAL throw 阻止 boot;`stripe.ts:45` `verifyWebhookSignature` 也 `.trim()` 做纵深防御
